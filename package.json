{
	"name": "chromadb-client",
	"private": true,
	"scripts": {
		"dev": "pnpm -r dev",
		"build": "pnpm -r build",
		"frontend:dev": "pnpm -F @chromadb-client/frontend dev",
		"backend:dev": "pnpm -F @chromadb-client/backend dev",
		"docker:dev": "pnpm build && docker compose up --build -d"
	},
	"workspaces": [
		"apps/*"
	]
}
